load('polyin.mat', 'h');

%pgon = polyshape(h.Position(:,1),h.Position(:,2));
%plot(pgon);

Positions = [zeros(size(h.Position)); zeros(size(h.Position))];
for i =1:length(h.Position)
    Positions(((i-1)*2)+1, 1) = h.Position(i);
    Positions(((i-1)*2)+2) = round( (h.Position(i) + h.Position(i+1)) / 2);
end

T = Triangulation(h.Position(:,1),h.Position(:,2));
figure();
triplot(T)